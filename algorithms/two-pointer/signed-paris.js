export class IndexPair {
    constructor(a, b) {
        this.a = a;
        this.b = b;
        this.n = a.length - 1;
        this.m = b.length - 1;

    }
}
export const findSignedPairs = (arr, k) => {
    let IndexPair = new IndexPair();

    if (IndexPair.m == k) {
        
    }
}